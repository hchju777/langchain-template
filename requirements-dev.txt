# 개발·테스트 의존성
#
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# 테스트는 표준 라이브러리 unittest로 작성돼 있어 pytest 없이도 돕니다.
#
#   python -m unittest discover -s tests -t .
#
# pytest를 설치하면 같은 테스트를 그대로 실행하면서 더 나은 출력과
# 선택 실행(-k)을 쓸 수 있습니다.
#
#   pytest tests -q
#   pytest tests -k "override"
pytest>=8.0

# 커버리지 확인용 (선택)
# pytest-cov>=5.0
