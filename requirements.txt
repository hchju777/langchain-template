# 런타임 의존성
#
#   pip install -r requirements.txt
#
# Windows / Linux 모두 미리 빌드된 휠로 설치됩니다(컴파일러 불필요).
# 개발·테스트용은 requirements-dev.txt를 보세요.

# ── 코어 ──────────────────────────────────────────────────
langgraph>=1.2,<2
langgraph-checkpoint>=4.2,<5
langchain>=1.3,<2
langchain-core>=1.5,<2
pydantic>=2.13,<3
pydantic-settings>=2.15,<3

# ── 리포트 템플릿 ─────────────────────────────────────────
# 현재는 표준 라이브러리(string.Template)로 렌더링합니다.
# 설치하면 반복문·조건문이 있는 Jinja2 템플릿으로 갈아탈 수 있습니다.
jinja2>=3.1,<4

# ── 스케줄러 ──────────────────────────────────────────────
# 4.x는 아직 알파이고 API가 크게 다릅니다. 3.x 계열로 고정합니다.
APScheduler>=3.11,<4

# Windows 필수. zoneinfo가 시스템 tz 데이터베이스를 찾는데 Windows에는
# 없어서, 이게 없으면 config의 "timezone": "Asia/Seoul"이 실패합니다.
# Linux/macOS에서는 있어도 무해합니다.
tzdata>=2024.1

# ── LLM ───────────────────────────────────────────────────
# config의 llm.adapter가 "chat_model"일 때만 실제로 로드됩니다.
# 사내 게이트웨이가 OpenAI 호환이면 이것만으로 충분합니다
# (.env의 LLM_BASE_URL만 바꾸면 됩니다).
langchain-openai>=1.4,<2
# 다른 공급자를 쓴다면 해당 패키지를 추가하세요:
# langchain-anthropic>=1.0

# ── 저장소 어댑터 ─────────────────────────────────────────
# 지금은 전부 스텁이라 없어도 동작합니다.
# 실제 연결로 전환할 때 필요합니다 (src/infrastructure/stores.py 참조).
redis>=5.0                 # Redis — redis.asyncio
aiokafka>=0.14             # Kafka AdminClient — 오프셋 메타데이터만 읽음
httpx>=0.28,<1             # REST API
aiosmtplib>=3.0            # 메일 발송

# MongoDB — PyMongo의 AsyncMongoClient를 씁니다.
#
# motor는 2026-05-14에 deprecated 되었습니다(중요 버그 수정만 2027-05-14까지).
# 새로 시작하는 코드는 PyMongo Async를 써야 합니다:
#
#     from pymongo import AsyncMongoClient
#
# 상한(<4.17)은 아래 langgraph-checkpoint-mongodb가 요구하는 범위입니다.
pymongo>=4.12,<4.17

# ── 영속 체크포인터 ───────────────────────────────────────
# 없으면 checkpoint.backend를 "memory"로 두어야 합니다. 그 경우
# Time Travel·fork는 되지만 프로세스 재시작 후 재개가 불가능합니다.
# langchain-mongodb와 pymongo를 함께 끌어옵니다.
langgraph-checkpoint-mongodb>=0.4
